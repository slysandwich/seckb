# CredMaster

```
python3 credmaster.py --plugin o365 -u <users file> -p <passwords file> -a <user agents file> -m 20 -j 30 -d 30 --access_key <AWS API access key> --secret_access_key <AWS API secret access key> -o <ouput file>
```
